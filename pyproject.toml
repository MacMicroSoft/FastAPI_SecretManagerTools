[tool.poetry]
name = "app"
version = "0.1.0"
description = ""
authors = ["maksy <maksym.potapchuk.hp@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
fastapi = "^0.111.0"
uvicorn = {extras = ["standard"], version = "^0.30.1"}
fastapi-mail = "1.4.1"
passlib = "^1.7.4"
itsdangerous = "^2.1.0"
asyncpg = "0.29.0"
starlette-admin = "^0.14.0"
pytest = "^8.2.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"


[tool.poetry.scripts]
start = "app.main:app"
